from django.db import models

# Create your models here.
class schools(model, model):
    name = models.CharField(max_length=23)
    address = models.CharField(max_length=23)

class country(model, model):
    name = models.CharField(max_length=23)
    
